{
  "name": "massanapp",
  "private": true,
  "version": "0.0.0",
  "description": "MassanApp prototype — smoke-test harness. The frontend in web/ is buildless and does not depend on this package.json.",
  "scripts": {
    "smoke": "playwright test --config=tests/playwright/playwright.config.js",
    "test:unit": "node --test 'tests/unit/*.test.mjs'"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1",
    "http-server": "14.1.1"
  }
}
