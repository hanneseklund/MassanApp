# Revisit language support (issue #8)

Issue #8 (https://github.com/hanneseklund/MassanApp/issues/8) received new
activity on 2026-04-18 after the original language-support task was completed
(see `agent/tasks_completed/add-language-support.md`).

Steps:
1. Review issue #8 (comments, reopened state, or edits) to determine what the
   new activity requires.
2. Verify the current English/Swedish language-switching implementation still
   works and check whether new requirements have been raised (e.g. missing
   translations, additional languages, UX changes, persistence across
   sessions).
3. Address any gaps surfaced by the new activity, then move this task to
   `agent/tasks_completed` with `git mv`.

If the new activity turns out to need clarification from the user, move this
task to `agent/tasks_blocked` with a note describing the open question.
