# Realistic food images and full menus

Address issue #24 (https://github.com/hanneseklund/MassanApp/issues/24).

Two parts:

1. **Replace food images with more realistic ones.** Source them from the web
   or AI-generate — whichever fits best per item. Current placeholder/stylized
   images should be swapped for photographs (or photorealistic renders) that
   clearly depict the dish.

2. **Make food menus complete menus.** Each menu should include drinks and
   sides where applicable, not just the main item. Review the existing food
   ordering data and expand entries so a customer sees a full offering
   (main + sides + drinks) rather than a bare dish.

When complete, move this file to `agent/tasks_completed/` via `git mv`.
